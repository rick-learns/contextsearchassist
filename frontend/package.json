{
  "name": "contextsearch-assist",
  "version": "1.0.0",
  "description": "ContextSearch Assist - Find related Jira tickets instantly with contextual search",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.10.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "PORT=5000 HOST=0.0.0.0 react-scripts start",
    "build": "react-scripts build",
    "build:extension": "INLINE_RUNTIME_CHUNK=false react-scripts build && cp -r build/* public/",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}